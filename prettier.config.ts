import { type Config } from "prettier";

const config: Config = {
  trailingComma: "es5",
  singleQuote: true,
  printWidth: 100,
  semi: true,
  tabWidth: 2,
  useTabs: false,
  bracketSpacing: true,
  arrowParens: "avoid",
  endOfLine: "lf",
};

export default config;
